{
    "name": "my-project",
    "version": "1.0.0",
    "devDependencies": {
        "babel-cli": "^6.0.0",
        "mongodb": "^2.2.11"
    },
    "scripts": {
        "build": "babel src -d build"
    }
}
